# Cloudflare Pages deploy command:
#   npx wrangler pages project create babel
#   npx wrangler pages deploy . --project-name babel
#
# Only static files are deployed (server/ runs on Railway).
# After deploying, set window.BABEL_SERVER_URL in index.html
# to your Railway WebSocket URL.
