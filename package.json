{
  "name": "babel",
  "version": "0.1.0",
  "description": "The Wikipedia Game set inside the Library of Babel — multiplayer browser game",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "node server/index.js",
    "start": "node server/index.js",
    "build": "echo 'No build step needed'",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "colyseus": "^0.15.0",
    "@colyseus/schema": "^2.0.0",
    "@colyseus/ws-transport": "^0.15.0",
    "express": "^4.18.0"
  }
}
